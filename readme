## TODO

* Moniter (Marvin Chung)
	* an interface contains following function:
		* view status: 
			ip of dhcp server
			number of childrn
			child status(DISCOVER, getip, .....)
		* send/recv message to/from children and moniter


* DHCP_RELEASE
	* a dhcp payload to release ip

* BAD_GUY (Sean)
	* an adversary to release other uses' ip
